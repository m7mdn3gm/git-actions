on:
  push:
    brance: 
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3

      - name: "Login to Dockerhub"
        run: docker login --username os2022 --password dckr_pat_I3QC-ypam2RD7fhlAz4-MKXpBUY 
