on:
  push:
    brance: 
      - master


jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name: "testing github actions"
        run: pwd

      - name: "printing a word"
        run: echo start
